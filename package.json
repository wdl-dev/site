{
  "name": "site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:logo": "node scripts/build-logo.mjs",
    "deploy": "wdl deploy . --ns site",
    "dry-run": "wrangler deploy --dry-run --outdir=.deploy-dist"
  },
  "devDependencies": {
    "wrangler": "^4.102.0",
    "@wdl-dev/cli": "^1.2.1"
  }
}
